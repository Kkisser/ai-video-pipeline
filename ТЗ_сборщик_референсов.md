# ТЗ — Приложение «Сборщик референсов»

> Черновик для правок. Помечай `[ПРАВКА: ...]` прямо в тексте — я учту.

## 1. Цель
Локальное приложение, которое по заданным параметрам само собирает **базу
референс-роликов** из соцсетей (TikTok / Reels / YouTube), даёт удобно их
**отбирать, скачивать и разбирать**, и работает под **любую нишу/бренд** через
профили (REVYLINE — лишь один из профилей, не хардкод).

## 2. Зачем
Автоматизировать шаг «поиск + разбор референса» (блок 1 конвейера) и сделать его
универсальным, чтобы переиспользовать под разные проекты.

## 3. Сценарий работы (как пользуюсь)
1. Вставляю **API-токен** (Apify / EnsembleData) — хранится локально.
2. Выбираю **профиль** (ниша/бренд) или создаю новый.
3. Задаю **источники**: ключевые слова, авторы (@), хэштеги, звуки; платформы.
4. Задаю **фильтры**: язык, за N дней, вертикаль, ≤N сек, мин. просмотры.
5. Жму **«Обновить»** → приложение тянет свежие ролики в базу-таблицу.
6. Смотрю таблицу, **отмечаю** понравившиеся.
7. **«Скачать выбранные»** → yt-dlp качает локально.
8. **«Разобрать»** → whisper (реплики) + LLM (структура сцен) → черновик разбора.
9. *(позже)* **«Адаптировать под профиль»** → LLM + правила профиля → сценарий-JSON.

## 4. Функции
**Обязательно (must):**
- Хранение токенов локально (`.env`, не в git).
- Источники: ключевик, авторы, хэштеги, звуки — платформа **TikTok (Apify)**.
- Фильтры: язык (кириллица), за N дней, вертикаль, длительность, мин. просмотры.
- **База-таблица** (SQLite) со статусами.
- **Веб-панель** (localhost) — просмотр таблицы + отметки + кнопка «Обновить».
- **Скачивание** выбранных (yt-dlp).
- **Профили бренда** (несколько, переключаемые).

**Позже (later):**
- Разбор: faster-whisper + LLM; транскрипт напрямую из Apify (где есть).
- Источники Instagram/Reels, YouTube.
- Адаптация → сценарий-JSON (стык с блоком 2).
- Комменты (понять, за что ролик залетел).
- Автообновление по расписанию.

## 5. База данных (таблица референсов)
Поля: `id, платформа, url, автор, текст, просмотры, лайки, комменты, дата,
длительность, ширина, высота, хэштеги, звук, статус (new/picked/downloaded/analyzed),
файл (локальный путь), транскрипт, разбор, профиль, добавлено`.

## 6. Профиль бренда (универсальность)
Поля: `имя, описание стиля, персонажи, тон, правила (обход модерации), товары,
эталонные ссылки/референсы`. Применяется на шагах разбора и адаптации.
Хранить как отдельные файлы/записи; переключение в один клик.

## 7. Технологии (предложение)
- **Python** (как flowbatch), веб-панель на встроенном http-сервере (как
  `flowbatch/web.py`) или FastAPI.
- **SQLite** — база.
- Источники: **Apify API** (акторы), **yt-dlp** (скачивание/метрики).
- Разбор: **faster-whisper** + LLM (Claude / Gemini).
- Токены — `.env` (gitignore).

## 8. Ограничения / риски
- Токены только локально, никогда в git.
- TikTok-дискавери — через Apify (в пределах $5 free, дальше платно).
- «За неделю + узкая ниша» = мало результатов; расширять запрос/хэштеги.

## 9. Этапы сборки (по порядку, с отчётом после каждого)
1. **Каркас** — проект + SQLite-схема + чтение токена из `.env`.
2. **Источник Apify** — ключевик/хэштег/автор → запись в базу + фильтры.
3. **Веб-панель** — таблица + отметки + «Обновить».
4. **Скачивание** выбранных (yt-dlp).
5. **Профили бренда** — создание/переключение.
6. *(later)* Разбор whisper + LLM.
7. *(later)* Instagram/YouTube, адаптация → JSON.

## 10. Открытые вопросы (реши/поправь)
- [ ] Начинаем с **Apify** (у нас уже есть токен и рабочий скрипт) — ок?
- [ ] Нужен ли EnsembleData сразу или потом?
- [ ] Веб-панель или сначала простой CLI + CSV/таблица?
- [ ] Instagram/Reels — в must или later?
- [ ] Где хранить базу и скачанные файлы (папка проекта?).
